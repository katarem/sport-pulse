api:
  football:
    key: ${API_FOOTBALL_KEY}
    url: ${API_FOOTBALL_URL}
server:
  port: 8083
spring:
  application:
    name: ms-teams
  management:
    endpoints:
      web:
        exposure:
          include: health