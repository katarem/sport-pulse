api:
  allowedServices: [MS_DASHBOARD]
  football:
    key: ${FOOTBALL_API_KEY}
    url: ${FOOTBALL_API_URL}
jwt:
  secret: ${JWT_SECRET}
server:
  port: 8080
spring:
  application:
    name: ms-teams
  management:
    endpoints:
      web:
        exposure:
          include: health