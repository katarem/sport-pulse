api:
  football:
    url: https://v3.football.api-sports.io
    api-key: ${FOOTBALL_API_KEY}

server:
  port: 8085

spring:
  application:
    name: ms-fixtures
  management:
    endpoints:
      web:
        exposure:
          include: health