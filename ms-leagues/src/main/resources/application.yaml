spring:
  application:
    name: ms-leagues
  management:
    endpoints:
      web:
        exposure:
          include: health
server:
  port: 8082
api:
  football:
    url: ${FOOTBALL_API_URL}
    key: ${FOOTBALL_API_KEY}
  allowedServices: ["MS-STANDINGS","MS-FIXTURES", "MS-LEAGUES", "MS-TEAMS", "MS-NOTIFICATIONS", "MS-DASHBOARD"]
auth:
  url: http://localhost:8081
